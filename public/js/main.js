// Mobile menu toggle functionality (will add later)
document.addEventListener('DOMContentLoaded', function() {
    console.log('Script loaded');
    // We'll add more functionality here later
});